on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.GET_CPANEL_REACT_SERVER }}
        username: ${{ secrets.GET_CPANEL_REACT_USER }}
        password: ${{ secrets.GET_CPANEL_REACT_PWD }}
        protocol: ${{ secrets.GET_CPANEL_REACT_PROTOCOL }}
        local-dir: ./build/